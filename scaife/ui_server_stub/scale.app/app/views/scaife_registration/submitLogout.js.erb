$(document).ready(function() {
  <% if @scaife_response.present? %>
    <% if @scaife_response === "Log in to SCAIFE first" %>
      $("#connect_to_scaife").click();
      $('#scaife-login-modal').on('hidden.bs.modal', function () {
        if(!$('#scaife-register-modal').is(':visible')) {
          window.location.reload();
        } else {
          $('#scaife-register-modal').on('hidden.bs.modal', function () {
            window.location.reload();
          });
        }
      });
    <% else %>
      <% if not ["User Successfully Logged Out", "SCAIFE registration server offline"].include? @scaife_response %>
        alert("<%= j @scaife_response %>");
      <% end %>
      window.location.reload();
    <% end %>
  <% end %>
});
